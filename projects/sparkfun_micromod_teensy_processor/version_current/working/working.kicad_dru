(version 1)
(rule "class 1:1"
  (condition "A.NetClass == '90Ohm' && B.NetClass == '90Ohm'")
  (constraint clearance (min 0.127mm)))

(rule "class 2:2"
  (condition "A.NetClass == 'Clock' && B.NetClass == 'Clock'")
  (constraint clearance (min 0.1778mm)))
